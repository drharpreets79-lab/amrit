# Globalization — Phase Execution Tracker

Companion to [PLAN.md](./PLAN.md) (Revision 3). Update the Status column as each phase lands.
Statuses: `not-started` · `in-progress` · `blocked` · `done`

## Pre-flight

| # | Item | Status | Notes |
|---|---|---|---|
| P0 | `git init` + baseline commit + branch-per-phase | **done** | Baseline `ff7ba04`; `.gitignore` excludes ~1.4 GB of `node_modules`/`release`/`.venv`/build output. Repo-local identity only — no global git config touched. |
| P1 | Decisions recorded | done | N-level tree · ISO 3166-2 + importer · i18n framework + English extraction · full white-label; new installations remain neutral until a deployment country is chosen |
| P2 | Rev 1 → Rev 2 audit: 9 world-usability gaps closed in plan | done | Profile authoring · multi-country · EUCAST · data licensing · Unicode scoping · epi-week/calendar · multi-timezone · onboarding · deployability |
| P3 | Rev 2 → Rev 3: GUI configurability + two-product separation | done | Phase 6b (admin GUI, both products) and Phase A (split `app/` + `server/` + `shared/`) |

## Recorded baseline (commit `ff7ba04`, pre-split)

Every later phase is measured against this.

| Product | Result |
|---|---|
| desktop app | 17 test files passed / 1 skipped · 100 tests passed / 2 skipped |
| web server | 23 tests, OK |

## Phases

| # | Phase | Risk | Status | Exit criteria (short) |
|---|---|---|---|---|
| A | Repository separation into two products | medium | **done** (`c537e76`) | Both suites identical to baseline · `pnpm run check` 0 · `pnpm run build` 0 · `smoke_e2e.py` ALL CHECKS PASSED · `sync_shared.py --check` in sync · `git log --follow` resolves across the move |
| 0 | Country Profile registry | low | **done** (`2eae6bc`) | Gate passed in both runtimes: all 250 selectable countries (249 ISO 3166-1) synthesize and validate. app 110 tests / server 35 tests, check + build 0, smoke passed |
| 1 | Electron admin-unit tree | medium | **done** (`91f8181`) | Fresh build and in-place lift both yield 36 L1 + 785 L2; LGD masters keep every row; both linkage directions covered. app 119 tests, check + build 0 |
| 2 | Electron surface de-Indianized | low | **done** (`f138776`) | India and a three-level RTL country tested side by side, including the rendered form; India wire output unchanged. app 132 tests, check + build 0 |
| 3 | Seed split + geo packs + importer | medium | **done** (`a5a3537`, `b3046c2`) | Core seed carries no geography · geo-pack format, loader, generator and runtime import · India pack · `desktop_app` dependency severed · **ISO 3166-2 pack now bundled**: 200 countries, 5044 units, level labels from the standard's own subdivision types |
| 4 | Django `geo` app + Site linkage | medium | **done** (`d1d625a`) | On a populated DB, migration 0006 linked **17/17** demo sites and every profile carrying geography; two-country fixture isolates correctly. server 53 tests |
| 5 | Django scope + RBAC generalization | **HIGH** | **done** (`1303356`) | Snapshot parity holds across **1196** values, 1196 recomputed before and after; non-Latin RBAC tests pass. server 70 tests. **No API break**: legacy spellings remain the storage form |
| 6 | Namespaces, branding, schema 1.1 | medium | **done** (`95524bc`) | India identifiers byte-identical to every previous hardcoded string; TESTLAND emits no `icmr`; both contract versions validate independently; `tools/check_country_neutral.py` passes. server 89 tests |
| 6b | Profile administration GUI (both products) | medium | **done** (`d083094`) | Editable from UI in both products, no restart; audited; SVG upload, `javascript:` URL and build-time fields all refused; unprivileged user gets 403 from **every** deployment route. server 118 / app 161 tests |
| 7 | i18n / l10n | low | **done** (`01b3ef5`) | 1114 strings across 15 namespaces; `eslint-plugin-i18next` reports **zero** remaining literals in the renderer; catalogue consistency gated by tests. app 187 tests under both profiles |
| 8 | Wire contract, hardening, demo data, tests | medium | **done** (`a2407e2`) | Enrolment authenticated, secure by default; deployed clients still register; golden datasets wired; both suites green under **IN and TESTLAND**. server 132 / app 164 tests |
| 9 | Locale-correct data semantics | medium | **done** (`d3578a1`) | ISO vs MMWR week goldens in both runtimes against the same fixtures; fiscal-year goldens for Jan/Apr/Oct starts; per-site timezone bucketing; EUCAST a first-class breakpoint source. app 211 / server 169 tests |
| 10 | Reference-data licensing and provenance | medium | **done** (`d2c9d5a`) | 11 datasets recorded, 9 bundled; `tools/check_data_licences.py` gates the build; terms surfaced in both products. **Owner decision: SNOMED ships enabled with a disclaimer, not gated.** app 217 / server 180 tests |
| 11 | Install, onboarding and deployment | medium | **done** (`c577160`) | Postgres service added, Dockerfiles reconciled, backup/restore, `manage.py bootstrap` verified on a genuinely fresh DB for a curated and an uncurated country; DEPLOYMENT + ONBOARDING docs. **Three insecure defaults fixed.** server 192 tests |
| 12 | Privacy, retention and legal configurability | low | **done** | Retention purge tested in both runtimes, dry run by default; erasure crypto-shreds and the chain still verifies; disclosure floor profile-driven and never loosenable; DPIA + records-of-processing templates. app 251 / server 208 tests under **IN and TESTLAND** |
| 13 | Universal address; `state`/`district`/LGD retired | medium | **done** (`9a14ac2`) | No column, enum, wire field or control in either product names a country's tier · one postal-address field set for all 250 countries, identical in both runtimes against a shared fixture (`address_reference.json`) · **patients get a residence** (town + postal code, never a street), coarsened by `privacy.patient_postal_code_digits` before any export · canonical event **2.0**, 1.0/1.1 withdrawn · scopes stored as `country`/`admin:<level>`, old spellings accepted on input only · app 295 / server 233 tests under **IN and TESTLAND**, check + build 0, `check_country_neutral.py` extended and passing, `smoke_e2e.py` ALL CHECKS PASSED on a migrated database |

| 14 | Geocoding, address polish and brand assets | medium | **done** (`ed47fbb`) | Bundled geographic directory: 1,080,715 postal codes across 121 countries, 235,285 settlements across 246, subdivision centroids for 77% of ISO 3166-2 units, 21.4 MB for all 247 shards · resolver mirrored in both runtimes against one shared fixture · **facilities only, never a patient** · address labels translated from the pack's token instead of rendered raw · the ICMR emblem restored and reversed for the dark sidebar · manual rebuilt on a profile-driven brand template. app 316 / server 248 under **IN and TESTLAND**, check + build 0, every gate passing, `smoke_e2e.py` ALL CHECKS PASSED |

| 15 | International address and release hardening | medium | **done** (`8d914a2`) | Fresh desktop starts neutral and requires an explicit base country · postal/ZIP/PIN validation remains country-driven across all 250 format entries · full Google Plus Codes resolve offline for facilities, including countries with no postal system · reporting placement remains an N-level tree, separate from the postal address · patient residence still refuses street lines, coordinates and Plus Codes · package identity, country-wide role and guideline filter are neutral/profile-driven · app **368 passed / 3 skipped**, server **339 passed / 1 skipped** under default and TESTLAND · TypeScript/lint/build/package, Python dependency audit, OpenAPI, deploy checks, Docker build/runtime, shared sync/licence/neutrality gates and migrated smoke flow all pass |

| — | Outbreak detection, demonstration network, manuscript | medium | **done** (`b93ddf3`) | Not a numbered phase — recorded here for the first time. Kulldorff space-time permutation scan statistic in both runtimes, row-level scanning local and the portal receiving aggregates only · four-city demonstration network, 10,000 isolates per site over two years, organism mix by specimen and real co-resistance · manuscript draft and its phase-wise publication plan · WHONET/SaTScan evaluation literature. **No outbreak is seeded in the demonstration data**, so the outbreak page ships empty — closed by Phase 32 of [the expansion plan](../expansion/PLAN.md) |

## Continues in

Phases 16–34 are planned in [`docs/expansion/PLAN.md`](../expansion/PLAN.md) and tracked in
[`docs/expansion/PHASE_STATUS.md`](../expansion/PHASE_STATUS.md): five platforms, HL7/LOINC/ICD compliance,
a detector family, and outbreak-seeded synthetic data with a pre-registered benchmark.

## Post-split layout

```
app/       desktop application      (was electron_app/)
server/    web server               (was Server/)
shared/    contracts + golden datasets + VERSION, vendored into both
tools/     generate_catalog_seed.py, sync_shared.py
```

Vendored copies live at `app/resources/shared/` and `server/amrit_central_server/shared/`.
Never edit them — edit `shared/` and run `python3 tools/sync_shared.py`.

## Corrections found during execution

Plan claims that contact with the code disproved. Fixed in PLAN.md Rev 3.

| Claim | Reality |
|---|---|
| Phases 13, 14 and 15 recorded as **done** | They were complete in the working tree and **never committed**. HEAD sat at Phase 12 (`9fd6417`) with 161 modified, 108 untracked and 8 deleted paths — `outbreak-detection.ts`, `demo-population.ts`, `breakpoint-mapping.ts`, `geo-directory.ts`, the whole 21 MB geographic directory and `paper/` among the untracked. Three phases of work and the evidence for their exit criteria were unversioned. Found on 14 August 2026 while starting Phase 16. |
| Per-phase commits assumed reconstructable from a merged working tree | Only approximately. Whole-file attribution works — `database.ts` is 226 address lines against 6 elsewhere — but `services.ts` carries 54 Phase-13 lines and 77 Phase-15 lines in one file, and `index.ts` and `types.ts` are likewise interleaved. Hunk-level attribution has no ground truth. Commits `9a14ac2`, `ed47fbb` and `8d914a2` therefore carry correct file attribution and **do not build individually**; only `b93ddf3` onward is green. Unlike Phases A–11, they are not bisectable. A phase's commit has to be made when the phase lands, not reconstructed later. |
| Every gate passing at Phase 15 | `tools/check_country_neutral.py` fails on the tree as committed: `app/src/main/demo-population.ts:325` hardcodes `country: 'India'` instead of reading the active profile's `country_name`. That file was untracked, so it had never been through the gate — which is exactly the failure mode CI exists to remove. |
| "no `.env.example`" (gap #9, Phase 11) | `server/amrit_central_server/.env.example` **exists**, and already declares `POSTGRES_DB/USER/PASSWORD/PORT` plus a comment referencing a `docker-compose.override.yml` that is not in the tree. Extend it; do not replace it. |
| Single server Dockerfile | There are **two** (`server/Dockerfile`, `server/amrit_central_server/Dockerfile`) plus a stray `server/amrit_central_server/Dockerfileh` typo file. Reconcile in Phase 11. |
| — | `git mv Server server` fails on macOS (case-insensitive FS). Two-step rename via a temporary name is required. |
| "~249 countries" was an estimate | Exactly **249** ISO 3166-1 entries, **250** selectable. The bundled WHONET country code set has 252 rows: 249 ISO + Kosovo (user-assigned XKX, usable) + two **organisations**, WHO and FAO, which must never be offered as countries. |
| Assumed ICU gives canonical IANA zones | It does not. Node 22's ICU reports `Asia/Kolkata` **as** `Asia/Calcutta`, and which name appears depends on the ICU build — a checked-in artifact would drift across Node versions and hand Python's `zoneinfo` non-canonical ids. `tools/generate_country_reference.mjs` canonicalises explicitly. |
| Assumed one timezone per country profile | 34 entries span several zones. Taking `zones[0]` made `America/Adak` the US default. Those now get `timezone: null` + `timezone_ambiguous: true` and must choose explicitly — confirming gap #7 empirically. |
| Plan put ISO 3166-2 subdivision category names in Phase 0 labels | Not available offline yet. Synthesized profiles get one generic `Administrative area` level; the real label lands in Phase 3 with the ISO 3166-2 geo pack. |
| Plan's Phase 1 backfill sat in `migrate()` alone | `initialize()` runs `migrate()` **before** `seedPackagedCatalogue()`, so a fresh database has no geography to lift at that point. The backfill runs in both places; it is a no-op once the tree exists. |
| Catalogue seeding assumed on in tests | It is opt-in — `new AMRITDatabase(path, { seedCatalog: true, catalogSeedPath })`. A test that omits it gets an empty `master_states`. Any later phase writing geography tests must pass these options. |
| `admin_levels[].required` read as "events must carry this" | It means a **laboratory** must be placed at that level. Feeding it into the One Health field definitions made `state_code` mandatory on every event and broke capture; many events are national or aggregate. Those fields stay optional whatever the profile says. |
| Non-Latin digit handling assumed trivial | `Number('٢')` is `NaN` — JS parses only ASCII digits. Digit value must be derived from the offset within the character's own Unicode decimal-digit block. Phase 9 depends on this being right. |
| Assumed an imaging library was available for logo re-encoding | Pillow was not installed, so the documented re-encode guarantee silently did not hold — a PNG with appended bytes was stored verbatim. Uploads now **fail closed** when it is missing, and Pillow is declared in `requirements.txt`. A guarantee is either enforced or the operation is refused. |
| Adding a capability in code was assumed sufficient | `has_cap` consults `RoleDefinition` **before** the `ROLE_CAPS` defaults, so a new capability is invisible until the seeded rows carry it. A superuser was refused by their own portal until migration `0008` granted it. |
| Three test files silently inherited the ambient `AMRIT_COUNTRY_PROFILE` | Found the moment the two-profile matrix ran: they exercise India's LGD geography and exporter output, so under TESTLAND they failed for the right reason (no India geo pack). They now pin the profile they test. A matrix that only ever runs one profile proves nothing. |
| i18n conversion assumed a bare `t('search')` was equivalent | It was an accessibility regression: `` label={`Search ${label.toLowerCase()}`} `` gave each multi-select search box its own name, and the replacement gave every one on a screen the identical label "Search". Restored with the field name interpolated. Extraction must preserve what a label *distinguishes*, not only what it says. |
| Breakpoint download allowlist assumed CLSI | It admitted only `clsi.org`, so a EUCAST deployment could not fetch a breakpoint table at all — the paid standard was the only route. Found by the TESTLAND matrix. Widened to both bodies, **fixed in code**: the allowlist exists so the app cannot be talked into downloading from an arbitrary address, and a profile-supplied host would defeat it. |
| `.env.example` and the entrypoint shipped working credentials | `AMRIT_SEED_DEMO` defaulted **on**, seeding demo users whose passwords are published in this repository; the superuser password defaulted to `admin`. A production `docker compose up` therefore created accounts anyone reading the source could use. Both now off by default, and the entrypoint refuses to invent a password. |
| `DATABASE_URL=sqlite:///…` was assumed to honour its path | `_database_from_url` **discarded** it: every sqlite URL resolved to `BASE_DIR/db.sqlite3`. Deployments pointing elsewhere silently shared one database, and several "fresh database" checks in this session were reusing the repo's. Fixed and covered by a test. |
| Two server Dockerfiles, one a `gunicorn` variant | `Dockerfileh` ran the **WSGI** application, which drops the desktop WebSocket bridge entirely. Removed, along with the root stub, leaving the complete Dockerfile as the only one. |
| Test fixtures drifted from the curated profiles | `tests/helpers/profile.ts` lacked `epi_week_system` and `fiscal_year_start_month`, so profile-driven tests silently exercised defaults instead of India's and TESTLAND's real values. Fixtures must mirror the checked-in profiles. |
| — | Pre-existing date bug found in passing: an unpadded ISO date (`2026-3-4`) never matched `normalizeDate`'s pattern, fell through to `new Date()`, and could report the **previous day** after UTC conversion. Fixed. |
| Plan referred to the sites app as `sites` | Its Django app label is **`amrit_sites`** (`sites.apps.SitesConfig`). `makemigrations sites` fails. Phase 5 migrations must use `amrit_sites`. |
| Plan called a stored-scope rewrite the "minimum unavoidable break" | **Avoided entirely.** Canonical spellings are accepted on input everywhere, but levels 1 and 2 are still *stored* as `state`/`district`, so no consumer reading raw `scope_type` breaks. Only levels beyond the second store a canonical value, and none existed before. |
| Fan-out driven by the configured profile alone | A deployment with `AMRIT_COUNTRY_PROFILE` unset resolves the one-level fallback profile, which silently stopped district snapshots being produced. `refresh_levels()` unions the profile's levels with the levels the data actually uses. Caught only because the parity harness counts recomputed rows. |
| Phases 1–5 called the India-shaped columns a compatibility shim | They were, for one phase. Kept past that they became the thing they replaced: `Site.state`, `laboratory.state_lgd_code` and `national_events.state_code` were still what a three-level country could not use and a one-level country could not fill. Phase 13 removed them. A shim needs the phase that deletes it scheduled at the same time it is introduced. |
| "Where is this facility" was assumed to be the same question as "which unit does it report under" | It is not, and conflating them is why the FHIR exporter filed a commune as a `district` and a region as a `state`. A facility's postal town and its reporting unit are frequently different places, so they are now separate fields with separate rules. |
| Free-text `address` was assumed to be parseable into components | No parsing rule for an arbitrary address holds worldwide. The migration puts the old blob in the **first street line** — the only field it can honestly become — rather than guessing which part was a city. |
| Token substitution alone was assumed to render an address | `%C, %S %Z` with no admin area rendered `Atlanta, 30329`: the separator belonged to a field that was not there. Substitution now drops the separator that preceded an empty token, in both runtimes, and the shared fixture pins it. |
| Assumed a country's postal pattern could be tested as-is | The pack's patterns match a whole code, so an unanchored test accepted `XX682011XX` for a five-digit country. Both runtimes anchor; a pattern the engine cannot compile passes rather than becoming a data-entry wall. |
| Two seeded roles differed by more than a level's name | `state_health_officer` and `district_health_officer` had **identical** capability sets and differed only in which tier they named, so collapsing them into `admin_officer` — whose depth comes from the operator's own unit — lost nothing. Checked before merging them, not assumed. |
| Dropping a scope column was assumed to be a display change | It is an access-control change. An operator whose old level name matched no unit now has no scope, so they see **nothing** until someone links them. That is the safe direction and the migration says so out loud, but it must be a deliberate choice, not a side effect. |
| Patient geography was assumed to be covered by the facility work | It was not: `isolates` still carried `patient_state` and `patient_municipality` after laboratories were done, and neither could hold a postal code — the one sub-city geography almost every country has and every clinic in a country writes the same way. A patient's place is a different object from a facility's address and needed its own pass. |
| A patient residence was assumed to be an address with the same rules | Two differences, both load-bearing. A country's `required` set must **not** be enforced — a laboratory often does not know the town, and refusing the record loses the AST result too — and street lines must be refused outright rather than merely unused, because a field that exists will be filled. |
| The PII guard was assumed to cover the postal code | It listed `postal_code` and matched keys by lower-casing and de-hyphenating, so FHIR's `postalCode` folded to `postalcode` and **passed**. Found by a test written to prove the boundary held. The guard now splits camelCase before matching. |
| Portal chrome was assumed to be branding-neutral after Phase 6 | It was not. The header hardcoded one country's emblem, the top strip named that country's government, the footer carried its street address, and the page title its acronym — on every installation on earth. Found by running the portal, not by reading it. All four now come from the country profile, with a text fallback: no emblem beats another country's emblem. |
| `STATICFILES_STORAGE` assumed to still configure static storage | Django 6 reads `STORAGES`; the old setting is **ignored**, so the portal was serving unhashed, uncompressed assets and `collectstatic` wrote no manifest. Also, the project package is not an installed app, so its branding asset was never collectable — a clean checkout shipped a portal with a broken emblem. Both fixed, and `collectstatic` is now an explicit deployment step in the manual. |
| A facility address was validated as strictly as a form entry | `seed_demo` crashed on a partial address: the site registry demanded every field India writes on an envelope. A registry legitimately knows a town before it knows a street. `clean_address` now separates *unstorable* (no country, unplaceable field, bad postal pattern — always refused) from *incomplete* (accepted; completeness is enforced in the form, where someone can be asked). |
| The PII guard's key matching assumed one spelling | It lower-cased and de-hyphenated, so FHIR's `postalCode` folded to `postalcode` and passed a blocklist written as `postal_code`. Now camelCase is split before matching. |
| One `branding.logo` field was assumed to mean one file | The two products resolve it in **different asset roots** — the renderer as `./resources/<file>`, the portal as `static('img/<file>')` — and neither checks the file is there. Pointing the profile at `icmr_logo.png` made the portal work and left the desktop with no emblem at all, silently. `tools/generate_brand_assets.py` now writes identical bytes into both roots and a test in each product fails if a shipped profile names a mark either one is missing. |
| A reversed mark was assumed to be a recolouring of the supplied artwork | The supplied ICMR master is **fully opaque** — no alpha anywhere — so knocking out the colour channels produced a solid white rectangle in the sidebar. Coverage has to be recovered from the ink's distance from the white it was printed on, measured on the least-bright channel: judged by luminance the orange in the lockup came out half transparent. |
| The address pack's `zipex` field was split like its other lists | `sub_isoids` and `languages` are tilde-separated; `zipex` is **comma**-separated. Every country's `postal_code_examples` was therefore a one-element list holding the whole thing, and an Indian operator was shown `110034,110001` as a single example code. Ireland's `A65 F4E2` contains no comma, which is why it looked correct wherever anyone checked. |
| The pack's field labels were treated as display text | They are the source dataset's stable tokens — `pin`, `zip`, `eircode`, `emirate`, `post_town` — and rendering them directly put **"pin"** and **"state"** on screen as field labels in a finished form. They are translation keys; the renderer resolves them through the `address` catalogue and `labelFor` is now only the English fallback. |
| A form asking for the reporting unit and the postal address read as one repeated field | Both name the same kinds of place, so "State / UT" above a postal "state" looked like the same question twice and was reported as a bug. They are genuinely different — a hospital's postal town and its reporting district frequently differ — so the screen now says which is which, and the chosen unit is *offered* to the address field rather than typed twice. |
| Labelling the reporting unit and the postal address distinctly was assumed to settle the repetition | It did not. Naming the two questions apart explained the duplication without removing it, and the operator was still asked for the state twice — once from a hierarchy dropdown, once on the envelope. A clerk with a letterhead in front of them can answer the address and nothing else, so the facility form now asks for street, town and postal code, and the reporting unit is derived: the postal directory resolves the administrative area, and the deepest matching unit becomes `admin_unit_id` and `admin_path`. The stored shape is unchanged — every scope filter still matches the same two fields — and whatever was filled in without being asked for is shown back, because derivation nobody can see is how wrong geography gets published. |
| GeoNames postal data was assumed to hold whole codes | For copyright reasons it carries only the **outward** part for the United Kingdom, Canada, the Netherlands and Ireland. Without a prefix fallback a London operator typing a perfectly valid `EC1Y 8SY` was told their own postcode does not exist. |
| Matching ISO 3166-2 subdivisions to GeoNames divisions by name was assumed to be straightforward | Half of them did not match. Two causes, both fixed: the countries with no postal data had no division *names* at all until `admin1CodesASCII.txt` was added, and GeoNames spells out the administrative noun the standard omits — "Municipality of Žalec" against "Žalec". Coverage went 46% → 71% → 77%. |
| `{# … #}` was assumed to comment out a block in a Django template | It is **single-line only**. A three-line comment in the portal's base template rendered as visible body text on every page of the portal. Found by looking at the screen, not the diff. |
| The desktop's `normalizeAddress` was assumed to mirror its Python twin | The Python side kept a resolved coordinate and the TypeScript side silently dropped it, so a facility placed on the map lost its point on save. Caught by a test written to prove a facility keeps what a patient must not. |
| A single-line input was assumed to be enough for a street address | `address_lines` is the one multi-line field, and rendering it in an `<input>` displayed two real lines glued together — "Department of MicrobiologyGovernment Medical College" — then flattened them into one on the next edit. |
| First parity harness compared stale rows against themselves | It reported a pass while 15 district scopes were not being recomputed at all. The harness now records whether each row was produced by the run, and fails on `no longer recomputed`. Any future scope refactor must keep that check. |

## Previously blocked, now resolved

| Item | Resolution |
|---|---|
| **Bundled ISO 3166-2 geo pack** | The owner confirmed an ISO licence is held. `tools/generate_iso3166_2_pack.py` builds the pack from the ISO 3166-2 tables in pycountry (a build-time dependency only). 200 countries, 5044 units; 50 territories have no subdivisions in the standard and keep an empty tree, reported rather than silent. Terms recorded in `shared/data-licences.json` and surfaced in both products: a deployment redistributing this software needs its own ISO licence, and the pack is a separate file it can remove. |
| **SNOMED CT licence position** | Owner decision: ships enabled with a licence notice recorded on every profile and called out in the licences view. This software grants no SNOMED licence. |

## Open items carried by the plan

| Item | Where | Owner | Status |
|---|---|---|---|
| `AllowAny` on `create_labcode` + `fetch_site_token` | `sites/enrolment.py` | — | **fixed** (`a2407e2`). Worse than recorded: `fetch_site_token` mints a bearer token *and rotates the old one*, so an unauthenticated caller knowing a lab code could impersonate a laboratory and cut off its sync in one request. Now requires `AMRIT_ENROLMENT_SECRET`; refuses (503) when unconfigured rather than allowing everyone. |
| URL-name collision: `fetch_site_token` registered under the name `"create_labcode"` | `central/urls.py` | — | **fixed** — the routes now have distinct names and reverse independently. |
| Logo upload attack surface — SVG rejection, magic-byte validation, server-side re-encode, pinned `Content-Type` | Phase 6b, both products | — | **fixed** — executable SVG is refused, raster bytes are decoded and re-encoded, and missing Pillow fails closed. |
| Admin-entered URLs (`identifier_namespace.base_uri`, `map.tile_url`) reach FHIR output and `<img src>` | Phase 6b | — | **fixed** — only absolute HTTPS URLs pass validation in either product. |
| Identifier-namespace change is not fully reversible for already-exported FHIR bundles | Phase 6b | — | **fixed as a provenance boundary** — changes are confirmed, audited and stamped `effective_from`; prior exports are immutable historical facts. |
| Unicode scoping bug: 10 `__iexact` name filters return zero rows for non-ASCII unit names | `central/roles.py`, `metrics/compute.py`, `dashboards/refresh.py`, `queries/views.py` | — | **closed** — Phase 13. Every one is gone: scoping is an `admin_path` prefix match on ASCII codes, and there is no name column left to fall back to. The only `__iexact` remaining in the server is a username uniqueness check |
| SNOMED CT redistribution licence position | `shared/data-licences.json`, profile `code_systems.snomed` | owner | **decided** — ships enabled with a licence notice recorded on every profile and called out in the licences view of both products. This software grants no SNOMED licence; a deployment establishes its own position. Revisit only if distribution terms change. |
| CLSI-only breakpoint sources; no EUCAST path | `app/src/main/services.ts` | — | **fixed** — EUCAST import, reviewed bundled fallback, official update route and activation gate are implemented. |
| `generate_catalog_seed.py` needs a `desktop_app/` project absent from this checkout | `tools/generate_catalog_seed.py` | — | **closed for runtime** — the application ships the country-neutral v2 catalogue; only the provenance-preserving archival rebuild tool accepts an explicit legacy source path. |
| `docker-compose.yml` defines no database service | `server/docker-compose.yml` | — | **fixed** (`c577160`) |
| Erasure vs. tamper-evident audit hash chain (crypto-shredding design) | One Health governance chain | — | **fixed** — erasure crypto-shreds protected detail while the verification chain remains testable. |
| Doc/code mismatch: `PRIVACY_AND_SAFETY.md` claimed Indian-identifier redaction the generic regexes do not implement | `app/docs/PRIVACY_AND_SAFETY.md` | — | **closed** — Phase 13. The doc now says what the code does (shape-matching patterns) and points a deployment at `banned_identifier_keys` for its own national schemes. The stale CLSI-only download claim was corrected in the same pass. |
| `golden-datasets/*.json` orphaned | `shared/golden-datasets/` | — | **fixed** (`a2407e2`) — now the source of truth for the AMC formulas |
| Decide whether `legacy_scope_type()` is applied on API output for the India profile | `central/scopes.py` | — | **closed** — canonical `country` / `admin:<level>` spellings are emitted; legacy spellings are input-only compatibility. |
| Drop legacy `master_states`/`master_districts` and `*_lgd_code` columns | both products | — | **closed** — runtime schema uses `admin_unit_id` / `admin_path`; withdrawn names remain only in one-way migration history and the legacy v1 seed used to lift old databases. |
